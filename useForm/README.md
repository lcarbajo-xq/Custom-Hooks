# useForm Hook

Ejemplo de uso:

```
const initialForm = {
    name:  '',
    edad: '',
    email: '',
    ....
}
const { formValues, handleInputChange, reset} = useForm( initialForm)
```

useCounter() // Recibe un valor por defecto
